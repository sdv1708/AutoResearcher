# ────────────────────────────────────────────────────────────────
# AutoResearcher – local development environment variables
# Copy to `.env` (never commit the real one)                      #
# ────────────────────────────────────────────────────────────────

# --- general ---
LOG_LEVEL=INFO

# --- embeddings / LLM ---
USE_VERTEX=false                   # TODO(cloud): set to true in Cloud Run
VERTEX_PROJECT_ID=                 # TODO(cloud): your GCP project id
VERTEX_REGION=us-central1          # TODO(cloud): match the region in Terraform

# --- auth ---
GOOGLE_APPLICATION_CREDENTIALS=    # local: path to JSON key; cloud: **unset**

# --- Cloud Run runtime (injected by Cloud Run UI or Terraform) --
SERVICE_ACCOUNT_EMAIL=             # TODO(cloud): autoresearcher-sa@<project>.iam.gserviceaccount.com